{
  "name": "aria2-api",
  "version": "1.0.4",
  "description": "aria2-api",
  "main": "index.js",
  "scripts": {
    "pub": "echo f|xcopy package.json dist\\package.json /Y && echo f|xcopy README.md dist\\README.md /Y && cd dist && npm publish --access public && cd.. && rmdir dist /s /q",
    "ver": "git add . && git commit -m \"publish to npm\" && npm version patch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aria2",
    "api"
  ],
  "author": "gin",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.3"
  }
}
